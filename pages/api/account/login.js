

export default (req, res) => {
    // const { name, password } = req.body
    // const user = db.find(user,{
    //     name,
    //     password
    // })
    
    // res.status(200).send(user)
    
}